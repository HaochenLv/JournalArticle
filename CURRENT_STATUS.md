# Current status — Stage 5 JB1+ correction complete

**Stage 5 complete (2026-09-17): a clear trade-off; journal position B.** Read [JB1_PLUS_EVIDENCE_REPORT.md](JB1_PLUS_EVIDENCE_REPORT.md) and [technical evidence](docs/JB1_PLUS_TECHNICAL_REPORT.md). Keep JB1 as the main research object and JB1+ as a mechanism-guided correction experiment; do not present it as a safer replacement.

One frozen formula scales only active-Prefill compute by its remaining lifetime fraction; intrinsic H remains full, Raw/Legacy trajectories unchanged. Protocol/workloads committed in e3887ca before outcomes. Reused2,050 Stage4 primary physical points plus130 A100 points, with zero old-reference reruns. Four new held-out workloads used500 new HELIX points/500 attempts, all successful, no retries/timeouts/failures. Each workload reached16 added intensities;37 unresolved intervals remain, maximum41.42%. No budget expansion.

Phase A Raw C1911→1604/O38→39, Legacy C2046→1724/O31→32. Phase B Raw C225→153 and Legacy C227→155, but both O7→17. Held-out winner agreement stays14/16 Raw and13/16 Legacy. Mean absolute capacity gap only improves41.11%→38.94% Raw and41.31%→39.14% Legacy; TPOT5 mean absolute error worsens and maximum overestimate rises+100%→+300%. Unsafe recommendations stay1/16, but w202/TPOT5 violations worsen1/48→9/48 as recommended intensity doubles. Average operating loss44.78%→38.87% does not erase that safety cost. The original completion-time drift remains.

All41 tests and integrity checks pass;5,867 protected historical files unchanged, old96 decisions/480 capacity entries reproduce, reference/pins/profiles unchanged. Two main-text candidate figures visually reviewed. Sequential evaluator timings stay in the same millisecond order; these are CPU planning timings, not GPU speedup. Reports retain all adverse cases, censoring and nonmonotonicity. **No JB1++, no further experiments, no manuscript drafted.**

## Historical Stage 4 completion


**Stage 4 complete (2026-09-17): strengthens current story within the tested scope.** Read [SLA_SENSITIVITY_EVIDENCE_REPORT.md](SLA_SENSITIVITY_EVIDENCE_REPORT.md) and [technical evidence](docs/SLA_REGIME_SENSITIVITY.md). This is a post-Stage-3 parameter supplement on the same six workloads, not independent held-out generalization. No manuscript prose or new algorithm was written.

Frozen eight SLA tuples, five shifts and Raw/Legacy semantics. Reused1,450 original physical points; added600 HELIX points/600 attempts, all successful, zero retries/timeouts/failures. Completed32,800 evaluator judgments and16,400 reference threshold classifications on2,050 physical points. Final grid sizes h101–h106:75/77/70/52/73/63. Four workloads reached24 added intensities; h104/h106 used11/13. Total600/720 budget. All observed transitions resolved for h103/h104/h106;27 unresolved intervals remain in h101/h102/h105, maximum18.92%, explicitly retained.

TPOT effects are nonmonotone. At TPOT5 the capacity-gap median is−95.08%, versus−75.00% at10; mean partition loss2.43% coexists with mean operating-point loss94.90%. h102 Raw at1.2/2.4 has arithmetic operating loss0% but an unsafe recommendation (3/54 violating requests), so safe loss100%; Legacy selects a safe alternative. At10 Legacy repairs h104/h105 winners but worsens h102 partition loss. Tightening TTFT to4.68 makes h102/h106 no-safe across the tested grid; relative quality is undefined. All24 Stage3 endpoint semantic decisions retain their winner, best sets and mismatch type.

All quality checks and36 unit tests pass.2,459 protected files remain byte-identical. One new main-text candidate figure (PNG/SVG) was visually reviewed. Old Stage2/3 evidence is not rewritten. **No further experiments** for these bounded findings; resolution limits do not authorize budget expansion.

## Historical Stage 3 status

**Stage 3 complete (2026-09-17). Main line: Yes, but modified.** Writing inputs are ready in [STAGE3_EVIDENCE_REPORT.md](STAGE3_EVIDENCE_REPORT.md); no complete manuscript was written. No further experiments needed for the stated scope.

Legacy-progress sensitivity evaluated3268 frozen nominal judgments using existing1634 physical reference records. No new reference runs, no grid extension, no baseline/protocol/raw overwrite. O23→17; C413→460;53 safe→unsafe changes. Tight agreement6/6 persists; relaxed0/6→2/6. Raw4 strict+2 tie-break mismatches; legacy3 strict+1 tie-break. h104/h105 improve; h102 worsens. Preserve8 no-safe configurations and3 nonmonotone reference sequences.

Decision-loss decomposition separates partition loss from recommended-load loss and exact reference safety. Raw relaxed losses are8.30%–26.16% versus68.95%–95.86%; h106 output is unsafe and its safe usable loss is100%. Mitigation is conditional reference-validation replay on a preconstructed reference-informed grid, not five-call planning from scratch.20%-target grid-snapped derating has nominal realized median46.53%, max79.87%, and8/28 partition changes after snapping.

31 tests pass; formal and Stage3 integrity records are in results/formal. Frozen source/raw manifests cover2387 files. Figures and claim labels updated. Stage2 stress remains35,688 rows and mitigation336 rows; no legacy-profile stress experiment is implied. Research monitoring remains paused; all required Stage3 work is completed in the current task.

The following checkpoints are historical execution records with historical terminology; they are not current claims.

## Historical checkpoints (superseded by completion above)

**Matrix complete; postprocessing in progress (2026-09-17,16:02 Beijing check).** All14 physical groups are complete:1634 saved physical points/3268 SLA pairs,0 failed points. The original195 pre-transfer rows are exactly unchanged, every candidate has its group common grid, and the frozen baseline hash is intact. The chained postprocessor observed matrix exit at08:00:17 UTC; RQ1–RQ3 summary generation succeeded and profile-mismatch evaluation is running. Mitigation, comprehensive formal verification, figures, sequential desktop timing, mechanism review and the14-section report remain outstanding. See `results/formal/desktop_matrix_complete.json` and the live `execution_workflow.json`. This supersedes the running-matrix statuses below; numeric summaries are not yet a final validated research report.

**Parallelism increased again at user request (2026-09-17).** Current matrix command is `scripts/formal_run.py --workers 12 --memory-budget-gib 24 --memory-reserve-gib 6`, superseding the8-worker/22GiB setting below. All1007 saved rows and every saved stage plan were checked unchanged before resuming. Memory admission estimates and the6GiB physical/commit headroom guard remain;12 is a ceiling, not guaranteed concurrency. Only unsaved in-flight attempts were interrupted. No simulator/evaluator, frozen protocol or inputs changed; this is a documented execution-resource amendment, and parallel runtimes are not controlled speedup evidence. Postprocessing stays at8 workers and formal timing remains sequential. Current PIDs/logs are in `desktop_resume.json`; evidence is in `desktop_parallel12_change.json`. The5-minute heartbeat was updated to follow these settings.

**Verifiable inspection history added (2026-09-17).** See `MONITORING_LOG.md` and `results/formal/monitoring/checks.jsonl` for actual inspections, with explicit manual/scheduled source and Beijing/UTC timestamps. The first entry is a real user-triggered manual check at14:28:04 Beijing:860 physical points/1720 SLA pairs,0 failed points,5/14 groups complete,8 active jobs, expected matrix/postprocessor processes alive. No historical scheduled checks were fabricated; an ACTIVE automation configuration alone is not evidence of execution. The5-minute heartbeat now must run `scripts/formal_healthcheck.py --source scheduled` on actual scheduled wakeups. User-triggered checks use `--source manual`. Latest health evidence is separate from final scientific validation.

**Follow-up cadence shortened to5 minutes (2026-09-17).** After the user asked whether30 minutes was too long, evaluated495 fresh desktop reference calls: median18.57s,90th percentile48.26s, maximum110.91s. At this check,690 physical points are saved with0 failures and5/14 groups complete. The active `journalarticle` heartbeat now checks every5 minutes for faults and research follow-through; healthy checks stay lightweight and quiet. No5-minute log update alone is grounds for interruption: retain600s/attempt and one retry, and inspect CPU, stage and queue before diagnosing a stall. The existing matrix-to-postprocessor handoff already polls every30 seconds and is independent of heartbeat cadence. This supersedes all earlier30-minute follow-up references below.

**Acceleration requested; memory-aware dispatch active (2026-09-17).** Native Windows now runs `formal_run.py --workers 8 --memory-budget-gib 22 --memory-reserve-gib 6`. Eight is the maximum, not an unconditional count: each physical group reserves an estimated512MiB +0.30MiB/output token (rounded up to256MiB); the scheduler rotates across groups and only admits jobs that fit the22GiB total and live physical/commit headroom guard. Large jobs may therefore use fewer slots while short jobs fill available space. These are conservative scheduling estimates, not hard memory limits or a measured speedup.28 tests pass.530 saved rows were snapshotted before switching;531 were preserved by shutdown, including a last completion; all prior rows and the original195 are exactly unchanged. New process/log locations and live scheduler snapshot are in `results/formal/desktop_resume.json`. The active30-minute heartbeat has been updated to this command and these logs; it must not revert to unbounded8-worker execution or the earlier fixed4 setting. Scientific inputs, reference implementation, grids,600s attempt limit and one retry remain unchanged. Older entries below record prior execution phases.

**Automatic desktop follow-up authorized and active (2026-09-17).** The user explicitly approved automatic continuation. A30-minute heartbeat named `JournalArticle 主机第二阶段续跑` (automation id `journalarticle`) is attached to the current desktop task; it checks existing processes, resumes only unfinished work when necessary, and continues the research review/report after numeric processing. It stays quiet while healthy computation continues and pauses after the final report and push. Latest inspection:373 saved physical points/746 SLA pairs,0 recorded failures,0 of14 groups fully refined; matrix and chained postprocessor both alive, with ample memory at4 workers. This supersedes the earlier pending-authorization note below. The formal matrix is still incomplete and no final research conclusion is claimed.

**RUNNING on native Windows with4 reference workers (2026-09-17).** The user requested checking native Python instead of installing WSL.25 tests pass, including real child termination on timeout and non-destructive Windows process probing. A fresh14-request reference trace exactly matches the inherited per-request metrics and final time; both SLA reference verdicts and all evaluator fields except runtime also match. Evidence: `results/formal/desktop_host_check.json`. Execution-only portability fixes preserve600s/one retry, frozen JB1, protocol, shared plans and all195 inherited rows. The initial8-worker run saved8 new successful points, then approached this32GB host's physical/commit memory limits; the matrix resumed with4 workers from203 saved points/406 SLA pairs and0 recorded failures. The original195 rows are exactly unchanged. See `desktop_resume.json` and `desktop_memory_restart.json` under `results/formal`; the postprocessor is attached to the new matrix PID. The previous WSL blocker below is superseded; no WSL installation is required. Post-matrix analysis and research review remain outstanding. A periodic Codex research follow-up was not created: automatic approval review requires explicit user confirmation for that persistent automation; the existing matrix and chained postprocessor are running independently of it.

**Desktop intake verified; execution blocked on missing WSL2 (2026-09-17).** The Windows destination cloned the handoff commit and verified all6 frozen workloads,874 pre-transfer cache records,195 saved physical points and390 SLA pairs, with0 recorded failed points. Saved plans and incomplete probes match the handoff exactly. Windows checkout line endings were restored to the committed bytes; frozen JB1 SHA256 matches. See `results/formal/desktop_intake.json`. No experiments were rerun. Installation of WSL2/Linux requires confirmation because it changes host system components beyond this repository and may require a restart. Linux dependency setup/tests and the fresh cross-machine metric/verdict comparison remain pending; the matrix and postprocessor have not started on the desktop. The laptop must remain paused.

**PAUSED at user request for transfer from laptop to desktop.** All laptop campaign processes and its automatic follow-up are stopped.195 physical reference runs /390 SLA pairs are saved, with0 recorded failed points. Do not automatically resume on the laptop. Read `HANDOFF_TO_DESKTOP.md` and `results/formal/machine_handoff.json` before resuming on the destination machine. The older running-state description below records the pre-pause phase.

The journal research question is locked: judgement reliability → sampled capacity error → deployment decision transfer → controlled profile mismatch.

Baseline gate: JB1 is an explicitly recovered journal baseline, not the missing final CA source. It implements raw-compute progress, pre/post checks, atomic tied events, block-consistent KV and declared activation buffers. The2051-token ledger and24 trajectory pairs reproduce. Published edge pairs match0/12 with raw progress,12/12 when legacy fixed overhead is restored to progress. This distinction is now documented in BASELINE_IMPLEMENTATION_AUDIT; stage1 evaluator verdicts will not be reused as formal evidence.

Historical search is closed.22 unit/bridge tests pass. FI-JB1-v1 is frozen for six new workloads and76 configuration/regime combinations; all candidates share refined grids. Formal held-out runs are in progress with a shared eight-worker point queue. Completed points and interrupted-probe identities are retained; no interrupted point is assigned a verdict. Analysis, stress, mitigation, quality-check, tracing and sequential-timing scripts are prepared. One conservative mechanism has a metric-identical instrumented rerun; aggregate conclusions await the completed matrix. Missing historical bundles remain documented limitations, not blockers. Old repositories remain strictly read-only.

The postprocessing workflow has also been paused. It can be reattached to the new matrix process on the destination machine. See `results/formal/execution_workflow.json` and `docs/FORMAL_RUNBOOK.md`. Research review and the14-section FORMAL_EVIDENCE_REPORT remain outstanding; a computation-complete status must not be mistaken for a finished evidence assessment.
