# Stage 5: JB1+ Remaining-Prefill Blocking — frozen one-shot protocol

Machine authority: `config/jb1_plus_protocol.json`; source completion commit `4e579bba911ed807c42bd51c0c87d1ecabb17133`.

Only active-Prefill **compute debt** changes: remaining fraction = clip(1 − (t − prefill_start_s) / prefill_progress_duration_s, 0, 1). Multiply the request's frozen profiled Prefill compute by this fraction. Keep the entire H × input_tokens intrinsic term. Fixed and queue overhead enter only Legacy's progress duration, never directly enter blocking debt. Raw duration is compute; Legacy duration is compute + fixed + queue. At zero duration the fraction is zero at/after start. All other semantics remain frozen. Raw-Full, Legacy-Full, Raw-Remaining and Legacy-Remaining are compared without assuming any variant is better or safe. The new module supports an off mode that exactly reproduces frozen JB1, excluding runtime.

Phase A reuses all 2,050 Stage 4 primary heterogeneous physical points on its eight-SLA final common grids. Full variants are copied from verified Stage 4 records; only Remaining is newly evaluated. Supplement with all six original A100 control groups and the conservative h105/A100/fast/4.096, optimistic h103/A100/slow/.016 and nominal h101/relaxed shifts 0/2 mechanism cases. Existing HELIX records and traces are read-only; no reruns. This phase is post-hoc diagnosis.

Phase B freezes four generated finite workloads before any correction outcome. Seeds 201–204, durations 30/120/30/120 seconds, links fast/slow/slow/fast; target rate 0.5, same Azure constructor and paired token distribution as Stage 3. Offset counts **3-second source bins**, not seconds. Scan from bin 1140 after the last historical window. Use the constructor's cyclic source ordering: offsets 1140, 1150, 1190, 1200 (the last maps to bins 0–39). All actual source bins are disjoint from the six previous windows and each other. Empty windows would be skipped by deterministic input-only validation, never by outcomes. Exact requests, statistics, indices and fingerprints are frozen alongside the protocol. This is independent workload validation within the same generator/distribution, not external-data validation.

Only heterogeneous five-shift candidates −2…2 and TTFT 5.2 with TPOT .3/1.2/5/10. Initial common grid: .001, .004, .016, .064, .256, 1.024, 4.096, 16.384, 65.536. Outer legal bounds remain .0000625–262.144; no range extension is used. Retain censoring/no-safe rather than spending an unallocated endpoint-expansion budget.

Union both-direction adjacent boolean transitions over reference and four variants, all candidates and SLAs. For relative width > .025, propose round(sqrt(lower×upper),10), prioritize descending log width then ascending lower and deduplicate. Add to all candidates. Maximum 16 additional common intensities/workload and four rounds: 180 initial + at most 320 refinement physical points = **500 hard maximum**. Stop also on no unresolved transition or no legal midpoint; publish unresolved intervals. Failed labels stay null and never become unsafe.

Pinned HELIX and adapter, unchanged complete per-request metrics with full drain, 600 seconds/attempt and at most one identical-input retry. At most 12 workers, 24 GiB admission budget and 6 GiB reserve, original output-token cost model. Thresholds share each physical execution. New artifacts reside only under results/jb1_plus.

Report judgment O/C separately, capacity signed/absolute gap and all sequences/flags, decision agreement/strict/tie/rank, exact recommendation violations, partition loss, operating loss and safety-gated usable loss. Use the existing tie-break and loss definitions. Paired Full→Remaining comparisons retain newly optimistic cases. No composite score. Sequential fresh CPU evaluator timings on every initial Phase B point give median/p95 and event counts; these are not GPU serving speedups. Full-drain trajectory comparisons establish that reduced stopping can change event counts without changing progress semantics. Extra state is start/duration (existing compute retained); remaining fraction is computed per check in O(active Prefill).

All tracked source-stage files except the two mutable status/log files are hash protected. Baseline, profiles, dependency pins and reference caches remain unchanged. Freeze this document, machine protocol, manifest and workloads in a commit **before any JB1+ results**. Then implement, test, evaluate, verify and report once. No second correction, tuning, expanded SLA/workload/profile/model, or manuscript prose. Report an adverse result without changing the formula. Publication boundaries follow the Stage 4 record; only correction evidence is new.
